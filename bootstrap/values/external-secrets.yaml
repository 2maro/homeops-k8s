---
installCRDs: true

replicaCount: 1

serviceAccount:
    create: true
    name: external-secrets

resources:
    requests:
        cpu: 10m
        memory: 32Mi
    limits:
        cpu: 100m
        memory: 128Mi
